# URL absoluta do backend (incluindo /api). Em dev pode deixar em branco
# que o proxy do Vite encaminha /api → http://localhost:3001 automaticamente.
# Em produção (Vercel), aponta para o backend no Render:
#   VITE_API_BASE_URL=https://bonistore-api.onrender.com/api
VITE_API_BASE_URL=
